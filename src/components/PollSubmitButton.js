import React from 'react';

class PollSubmitButton extends React.Component {
    render() {
        return (
            <button className="btn btn-success">See who's the biggest bish</button>
        );
    }
}

export default PollSubmitButton;